#set( $updateList = {} )
#foreach( $entry in $ctx.args.updateCityInput.keySet() )
  #set( $discard = $updateList.put($entry, "'$ctx.args.updateCityInput[$entry]'") )
#end
#set( $update = $updateList.toString().replace("{","").replace("}","") )
{
  "version": "2018-05-29",
  "statements":   ["UPDATE City SET $update WHERE id=$ctx.args.updateCityInput.id", "SELECT * FROM City WHERE id=$ctx.args.updateCityInput.id"]
}