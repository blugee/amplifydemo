#set( $cols = [] )
#set( $vals = [] )
#foreach( $entry in $ctx.args.createCityInput.keySet() )
  #set( $discard = $cols.add($entry) )
  #set( $discard = $vals.add("'$ctx.args.createCityInput[$entry]'") )
#end
#set( $valStr = $vals.toString().replace("[","(").replace("]",")") )
#set( $colStr = $cols.toString().replace("[","(").replace("]",")") )
{
  "version": "2018-05-29",
  "statements":   ["INSERT INTO City $colStr VALUES $valStr", "SELECT * FROM City WHERE id=$ctx.args.createCityInput.id"]
}