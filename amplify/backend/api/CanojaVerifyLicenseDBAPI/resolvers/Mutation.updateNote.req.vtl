#set( $updateList = {} )
#foreach( $entry in $ctx.args.updateNoteInput.keySet() )
  #set( $discard = $updateList.put($entry, "'$ctx.args.updateNoteInput[$entry]'") )
#end
#set( $update = $updateList.toString().replace("{","").replace("}","") )
{
  "version": "2018-05-29",
  "statements":   ["UPDATE Note SET $update WHERE id=$ctx.args.updateNoteInput.id", "SELECT * FROM Note WHERE id=$ctx.args.updateNoteInput.id"]
}