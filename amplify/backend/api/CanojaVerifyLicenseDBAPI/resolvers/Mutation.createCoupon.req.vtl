#set( $cols = [] )
#set( $vals = [] )
#foreach( $entry in $ctx.args.createCouponInput.keySet() )
  #set( $discard = $cols.add($entry) )
  #set( $discard = $vals.add("'$ctx.args.createCouponInput[$entry]'") )
#end
#set( $valStr = $vals.toString().replace("[","(").replace("]",")") )
#set( $colStr = $cols.toString().replace("[","(").replace("]",")") )
{
  "version": "2018-05-29",
  "statements":   ["INSERT INTO Coupon $colStr VALUES $valStr", "SELECT * FROM Coupon WHERE id=$ctx.args.createCouponInput.id"]
}