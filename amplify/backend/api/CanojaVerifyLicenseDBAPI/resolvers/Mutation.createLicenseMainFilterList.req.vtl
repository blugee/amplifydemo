#set( $cols = [] )
#set( $vals = [] )
#foreach( $entry in $ctx.args.createLicenseMainFilterListInput.keySet() )
  #set( $discard = $cols.add($entry) )
  #set( $discard = $vals.add("'$ctx.args.createLicenseMainFilterListInput[$entry]'") )
#end
#set( $valStr = $vals.toString().replace("[","(").replace("]",")") )
#set( $colStr = $cols.toString().replace("[","(").replace("]",")") )
{
  "version": "2018-05-29",
  "statements":   ["INSERT INTO LicenseMainFilterList $colStr VALUES $valStr", "SELECT * FROM LicenseMainFilterList WHERE id=$ctx.args.createLicenseMainFilterListInput.id"]
}