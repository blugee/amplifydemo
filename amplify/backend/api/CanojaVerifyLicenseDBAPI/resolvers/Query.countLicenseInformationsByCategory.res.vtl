## Raise a GraphQL field error in case of a datasource invocation error
#if($ctx.error)
    $utils.error($ctx.error.message, $ctx.error.type)
#end

#set( $total = $utils.rds.toJsonObject($ctx.result)[0][0].total)

#set( $result = {
            "total": $total 
        } )
$utils.toJson($result)