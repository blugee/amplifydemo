#set( $cols = [] )
#set( $vals = [] )
#foreach( $entry in $ctx.args.createSubscriptionPlanInput.keySet() )
  #set( $discard = $cols.add($entry) )
  #set( $discard = $vals.add("'$ctx.args.createSubscriptionPlanInput[$entry]'") )
#end
#set( $valStr = $vals.toString().replace("[","(").replace("]",")") )
#set( $colStr = $cols.toString().replace("[","(").replace("]",")") )
{
  "version": "2018-05-29",
  "statements":   ["INSERT INTO SubscriptionPlan $colStr VALUES $valStr", "SELECT * FROM SubscriptionPlan WHERE id=$ctx.args.createSubscriptionPlanInput.id"]
}