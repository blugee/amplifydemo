#set( $cols = [] )
#set( $vals = [] )
#foreach( $entry in $ctx.args.createFeatureInput.keySet() )
  #set( $discard = $cols.add($entry) )
  #set( $discard = $vals.add("'$ctx.args.createFeatureInput[$entry]'") )
#end
#set( $valStr = $vals.toString().replace("[","(").replace("]",")") )
#set( $colStr = $cols.toString().replace("[","(").replace("]",")") )
{
  "version": "2018-05-29",
  "statements":   ["INSERT INTO Feature $colStr VALUES $valStr", "SELECT * FROM Feature WHERE id=$ctx.args.createFeatureInput.id"]
}