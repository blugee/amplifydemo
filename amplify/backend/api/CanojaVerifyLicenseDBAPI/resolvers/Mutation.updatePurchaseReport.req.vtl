#set( $updateList = {} )
#foreach( $entry in $ctx.args.updatePurchaseReportInput.keySet() )
  #set( $discard = $updateList.put($entry, "'$ctx.args.updatePurchaseReportInput[$entry]'") )
#end
#set( $update = $updateList.toString().replace("{","").replace("}","") )
{
  "version": "2018-05-29",
  "statements":   ["UPDATE PurchaseReport SET $update WHERE id=$ctx.args.updatePurchaseReportInput.id", "SELECT * FROM PurchaseReport WHERE id=$ctx.args.updatePurchaseReportInput.id"]
}