#set( $cols = [] )
#set( $vals = [] )
#foreach( $entry in $ctx.args.createLicenseCategoryInput.keySet() )
  #set( $discard = $cols.add($entry) )
  #set( $discard = $vals.add("'$ctx.args.createLicenseCategoryInput[$entry]'") )
#end
#set( $valStr = $vals.toString().replace("[","(").replace("]",")") )
#set( $colStr = $cols.toString().replace("[","(").replace("]",")") )
{
  "version": "2018-05-29",
  "statements":   ["INSERT INTO LicenseCategory $colStr VALUES $valStr", "SELECT * FROM LicenseCategory WHERE id=$ctx.args.createLicenseCategoryInput.id"]
}