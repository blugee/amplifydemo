#set( $cols = [] )
#set( $vals = [] )
#foreach( $entry in $ctx.args.createStateInput.keySet() )
  #set( $discard = $cols.add($entry) )
  #set( $discard = $vals.add("'$ctx.args.createStateInput[$entry]'") )
#end
#set( $valStr = $vals.toString().replace("[","(").replace("]",")") )
#set( $colStr = $cols.toString().replace("[","(").replace("]",")") )
{
  "version": "2018-05-29",
  "statements":   ["INSERT INTO State $colStr VALUES $valStr", "SELECT * FROM State WHERE id=$ctx.args.createStateInput.id"]
}