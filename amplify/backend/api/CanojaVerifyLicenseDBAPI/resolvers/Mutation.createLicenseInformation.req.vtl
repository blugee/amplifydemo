#set( $cols = [] )
#set( $vals = [] )
#foreach( $entry in $ctx.args.createLicenseInformationInput.keySet() )
  #set( $discard = $cols.add($entry) )
  #set( $discard = $vals.add("'$ctx.args.createLicenseInformationInput[$entry]'") )
#end
#set( $valStr = $vals.toString().replace("[","(").replace("]",")") )
#set( $colStr = $cols.toString().replace("[","(").replace("]",")") )
{
  "version": "2018-05-29",
  "statements":   ["INSERT INTO LicenseInformation $colStr VALUES $valStr", "SELECT * FROM LicenseInformation WHERE id=$ctx.args.createLicenseInformationInput.id"]
}